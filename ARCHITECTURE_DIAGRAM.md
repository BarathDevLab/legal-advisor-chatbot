# 🏗️ System Architecture Diagram

## Complete System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           USER INTERFACE                                 │
│                    (Next.js - Browser - Port 3000)                      │
│                                                                          │
│  ┌────────────┐  ┌──────────────┐  ┌────────────┐  ┌────────────┐    │
│  │  Login     │  │  Home/Chat   │  │  Upload    │  │  Status    │    │
│  │  Page      │  │  Interface   │  │  Docs      │  │  Page      │    │
│  └────────────┘  └──────────────┘  └────────────┘  └────────────┘    │
│         │               │                  │              │             │
└─────────┼───────────────┼──────────────────┼──────────────┼─────────────┘
          │               │                  │              │
          │               │                  │              │
          ▼               ▼                  ▼              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         NEXT.JS API ROUTES                               │
│                     (Server-Side - Port 3000)                           │
│                                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │ POST         │  │ POST         │  │ POST         │  │ GET        │ │
│  │ /api/session │  │ /api/chat    │  │ /api/upload  │  │ /api/status│ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬─────┘ │
└─────────┼──────────────────┼──────────────────┼──────────────────┼───────┘
          │                  │                  │                  │
          │ Proxy            │ Proxy            │ Proxy            │ Proxy
          ▼                  ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         FASTAPI BACKEND                                  │
│                    (Python - Port 8000)                                 │
│                                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │ POST         │  │ POST         │  │ POST         │  │ GET        │ │
│  │ /session/    │  │ /chat        │  │ /upload      │  │ /status    │ │
│  │ create       │  │              │  │              │  │            │ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬─────┘ │
│         │                  │                  │                  │       │
│         ▼                  ▼                  ▼                  ▼       │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    SESSION MANAGER                               │   │
│  │  • Tracks active sessions                                       │   │
│  │  • Stores conversation history                                  │   │
│  │  • Manages follow-up state                                      │   │
│  └──────────────────────┬───────────────────────────────────────────┘   │
│                         │                                                │
└─────────────────────────┼────────────────────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          │                               │
          ▼                               ▼
┌──────────────────────┐        ┌──────────────────────┐
│  WATSON ASSISTANT    │        │   CHAT PROCESSOR     │
│  (IBM Cloud)         │        │                      │
│                      │        │  1. Vector Search    │
│  • Intent detection  │        │  2. Watson Flow      │
│  • Entity extraction │        │  3. Answer Gen Check │
│  • Dialog management │        │  4. LLM Generation   │
│  • Follow-up logic   │        │                      │
└──────────────────────┘        └──────┬───────────────┘
                                       │
                        ┌──────────────┼──────────────┐
                        │              │              │
                        ▼              ▼              ▼
              ┌────────────┐  ┌────────────┐  ┌────────────┐
              │   FAISS    │  │  WATSONX   │  │  SENTENCE  │
              │  VECTOR    │  │   AI LLM   │  │ TRANSFORM  │
              │   INDEX    │  │  (GRANITE) │  │   EMBED    │
              └────────────┘  └────────────┘  └────────────┘
                    │
                    ▼
              ┌────────────┐
              │  PDF DOCS  │
              │  (Indexed) │
              └────────────┘
```

---

## Data Flow: Complete Chat Interaction

### Step-by-Step Flow

```
1. USER TYPES QUESTION
   "What is land tenure?"
   │
   ▼

2. FRONTEND (React Component)
   • Validates input
   • Shows loading state
   • Sends to API route
   │
   ▼

3. NEXT.JS API ROUTE (/api/chat)
   • Receives request
   • Proxies to backend
   • Returns response
   │
   ▼

4. FASTAPI ENDPOINT (/chat)
   • Validates session
   • Loads vector store
   • Starts chat processing
   │
   ▼

5. VECTOR SEARCH
   • Embed question → [0.234, 0.567, ...]
   • Search FAISS index
   • Retrieve top 3 chunks
   │
   ▼

6. WATSON ASSISTANT
   • Receives: "What is land tenure?"
   • Detects intent: legal_query
   • Response: "Let me check the documentation..."
   • Keywords: "checking" → should_generate_answer = TRUE
   │
   ▼

7. KEYWORD DETECTION
   processing_keywords = ['checking', 'searching', 'looking', ...]
   "checking" in response → GENERATE ANSWER
   │
   ▼

8. LLM GENERATION (Watsonx.ai Granite)
   Input:
   - Question: "What is land tenure?"
   - Context: Retrieved chunks from PDFs
   - History: Previous conversation

   Output:
   - Simplified legal explanation
   │
   ▼

9. RESPONSE ASSEMBLY
   {
     watson_stage: {
       response: "Let me check...",
       should_generate_answer: true
     },
     simplified_answer: "Land tenure means...",
     sources: [...]
   }
   │
   ▼

10. FRONTEND DISPLAY
    • Watson message
    • "Searching documents..." indicator
    • Simplified answer
    • Sources (expandable)
```

---

## Decision Tree: Should Generate Answer?

```
                    User sends message
                           │
                           ▼
                   ┌──────────────┐
                   │ Vector Search│
                   │  (always)    │
                   └──────┬───────┘
                          │
                          ▼
                  ┌───────────────┐
                  │ Watson Process│
                  └───────┬───────┘
                          │
                          ▼
            ┌─────────────────────────┐
            │ Check Watson Response   │
            │ for Processing Keywords │
            └──────────┬──────────────┘
                       │
         ┌─────────────┴─────────────┐
         │                           │
         ▼                           ▼
    Contains keyword?           No keyword?
    ['checking',                    │
     'searching',                   │
     'looking',                     │
     'reviewing']                   │
         │                           │
         ▼                           ▼
    ┌─────────┐               ┌──────────┐
    │ Is      │               │ Return   │
    │goodbye? │               │ Watson   │
    └────┬────┘               │ response │
         │                    │ only     │
    ┌────┴────┐               └──────────┘
    │         │
   Yes       No
    │         │
    ▼         ▼
 Return   Generate
 Watson   Answer
response    │
           ▼
    ┌──────────────┐
    │ LLM Generate │
    │ Simplified   │
    │ Explanation  │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ Return Full  │
    │ Response +   │
    │ Sources      │
    └──────────────┘
```

---

## State Management Flow

### Session State

```
┌──────────────────────────────────────┐
│        BACKEND SESSION STATE         │
├──────────────────────────────────────┤
│ session_id: uuid                     │
│ watson_session_id: uuid              │
│ messages: [...]                      │
│ conversation_context: [...]          │
│ awaiting_followup: boolean           │
│ chat_session_started: boolean        │
└──────────────────────────────────────┘
                  │
                  │ syncs to
                  ▼
┌──────────────────────────────────────┐
│       FRONTEND SESSION STATE         │
├──────────────────────────────────────┤
│ sessionId: string                    │
│ messages: Message[]                  │
│ conversationContext: string[]        │
│ awaitingFollowUp: boolean            │
│ isGoodbye: boolean                   │
└──────────────────────────────────────┘
```

### Conversation Context Management

```
Initial Question:
User: "What is land tenure?"
Backend: conversation_context = ["What is land tenure?"]
Frontend: Updates from backend

Follow-up:
User: "Tell me more"
Backend: Has context + adds new question
         conversation_context = ["What is land tenure?", "Tell me more"]
Frontend: Updates from backend

Combined Query to Watson:
"What is land tenure? Tell me more"
```

---

## Component Hierarchy

```
┌────────────────────────────────────────────────────────┐
│                     App (layout.tsx)                    │
│                                                         │
│  ┌────────────────────────────────────────────────┐   │
│  │            Home Page (home/page.tsx)            │   │
│  │                                                 │   │
│  │  ┌──────────────────────────────────────────┐ │   │
│  │  │         Header                            │ │   │
│  │  │  • Title                                  │ │   │
│  │  │  • Upload button (admin)                  │ │   │
│  │  │  • Logout button                          │ │   │
│  │  │  • Follow-up banner (conditional)         │ │   │
│  │  └──────────────────────────────────────────┘ │   │
│  │                                                 │   │
│  │  ┌──────────────────────────────────────────┐ │   │
│  │  │         Messages Area                     │ │   │
│  │  │                                           │ │   │
│  │  │  ┌─────────────────────────────────────┐ │ │   │
│  │  │  │ Message (User)                       │ │ │   │
│  │  │  │  • Content                           │ │ │   │
│  │  │  └─────────────────────────────────────┘ │ │   │
│  │  │                                           │ │   │
│  │  │  ┌─────────────────────────────────────┐ │ │   │
│  │  │  │ Message (Assistant)                  │ │ │   │
│  │  │  │                                      │ │ │   │
│  │  │  │  ┌────────────────────────────────┐ │ │ │   │
│  │  │  │  │ Watson Stage                   │ │ │ │   │
│  │  │  │  │  • Response text               │ │ │ │   │
│  │  │  │  │  • Processing indicator        │ │ │ │   │
│  │  │  │  │  • Follow-up questions         │ │ │ │   │
│  │  │  │  │  • Goodbye banner              │ │ │ │   │
│  │  │  │  └────────────────────────────────┘ │ │ │   │
│  │  │  │                                      │ │ │   │
│  │  │  │  ┌────────────────────────────────┐ │ │ │   │
│  │  │  │  │ Simplified Answer              │ │ │ │   │
│  │  │  │  │  • Legal explanation           │ │ │ │   │
│  │  │  │  └────────────────────────────────┘ │ │ │   │
│  │  │  │                                      │ │ │   │
│  │  │  │  ┌────────────────────────────────┐ │ │ │   │
│  │  │  │  │ Sources (expandable)           │ │ │ │   │
│  │  │  │  │  • Source 1 details            │ │ │ │   │
│  │  │  │  │  • Source 2 details            │ │ │ │   │
│  │  │  │  │  • Source 3 details            │ │ │ │   │
│  │  │  │  └────────────────────────────────┘ │ │ │   │
│  │  │  │                                      │ │ │   │
│  │  │  │  ┌────────────────────────────────┐ │ │ │   │
│  │  │  │  │ Suggested Questions            │ │ │ │   │
│  │  │  │  │  • Button 1                    │ │ │ │   │
│  │  │  │  │  • Button 2                    │ │ │ │   │
│  │  │  │  │  • Button 3                    │ │ │ │   │
│  │  │  │  └────────────────────────────────┘ │ │ │   │
│  │  │  │                                      │ │ │   │
│  │  │  │  • Disclaimer                        │ │ │   │
│  │  │  └─────────────────────────────────────┘ │ │   │
│  │  │                                           │ │   │
│  │  │  • Loading indicator (conditional)        │ │   │
│  │  │  • Suggested starter questions           │ │   │
│  │  └──────────────────────────────────────────┘ │   │
│  │                                                 │   │
│  │  ┌──────────────────────────────────────────┐ │   │
│  │  │         Footer                            │ │   │
│  │  │  • Input field (or hidden if goodbye)     │ │   │
│  │  │  • Send button                            │ │   │
│  │  │  • New Chat button (if goodbye)           │ │   │
│  │  └──────────────────────────────────────────┘ │   │
│  └────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────┘
```

---

## API Response Schemas

### POST /session/create Response

```typescript
{
  session_id: string; // UUID
  watson_session_id: string; // Watson session UUID
  welcome_message: string; // "Welcome to Legal RAG Navigator! 👋..."
}
```

### POST /chat Response

```typescript
{
  watson_stage: {
    response: string;                    // Watson's text response
    follow_ups: string[];                // Follow-up questions
    intents: Array<{...}>;              // Detected intents
    entities: Array<{...}>;             // Extracted entities
    actions: Array<{...}>;              // Actions triggered
    is_goodbye: boolean;                // Session ending?
    has_actions: boolean;               // Has actions?
    should_generate_answer: boolean;    // Trigger RAG?
  };
  simplified_answer: string | null;     // LLM generated answer
  sources: Array<{                      // Retrieved documents
    source_number: number;
    document: string;
    page: number;
    content: string;
  }>;
  timestamp: string;                    // ISO timestamp
  awaiting_followup: boolean;           // Waiting for user response?
  conversation_context: string[];       // Conversation history
}
```

---

## Processing Keywords

These keywords in Watson's response trigger RAG answer generation:

```
['checking', 'typing', 'pause', 'documentation',
 'searching', 'looking', 'reviewing', 'analyzing']
```

**Example:**

- Watson: "Let me **check** the documentation..." → RAG generates answer
- Watson: "Hello! How can I help?" → No RAG, just Watson response

---

## File Structure

```
project/
├── app/
│   ├── api/
│   │   ├── chat/route.ts        # Proxy to backend /chat
│   │   ├── session/route.ts     # Proxy to backend /session/create
│   │   └── upload/route.ts      # Proxy to backend /upload
│   ├── home/
│   │   └── page.tsx             # Main chat interface
│   └── upload/
│       └── page.tsx             # Document upload page
│
├── backend/                      # Python FastAPI backend
│   └── main.py                   # Main backend file
│
├── CHAT_FLOW_ANALYSIS.md         # This file - detailed analysis
├── FRONTEND_FIXES.md              # Applied fixes documentation
└── QUICK_START.md                 # Getting started guide
```
